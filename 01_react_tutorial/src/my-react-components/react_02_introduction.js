import React from 'react';


class ReactIntroduction extends React.Component {
    render() {
        return (
             <div>
                 <p>React Introduction:</p>
                 <h1>React creates a VIRTUAL DOM in memory.<br/>
                 React only changes what needs to be changed !</h1>
             </div>
             ); 
        
    }
}

export default ReactIntroduction 
